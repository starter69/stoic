
= form_for @exercise do |f|
  - if @exercise.errors.any?
    #error_explanation
      %h2= "#{pluralize(@exercise.errors.count, "error")} prohibited this exercise from being saved:"
      %ul
        - @exercise.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.hidden_field :user_id
  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :general_description
    = f.text_area :general_description
  .field
    = f.label :category
    = f.text_field :category
  - if current_user.admin
    .field
      = f.label "Publish this exercise globally to all Stoic Compass users"
      = f.check_box :global
  %h3 Exercise Questions
  #e_questions.field
    = f.fields_for :e_questions do |e_builder|
      = render "e_question_fields", :f => e_builder
    .links
      = link_to_add_association 'add question', f, :e_questions
  .actions
    = f.submit 'Save'
