= form_for @meditation do |f|
  - if @meditation.errors.any?
    #error_explanation
      %h2= "#{pluralize(@meditation.errors.count, "error")} prohibited this quotation from being saved:"
      %ul
        - @meditation.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :passage
    = f.text_area :passage
  .field
    = f.label :tag_list, "Category Tags"
    %br
    = f.label :tag_list, "(Separated by commas,"
    %br
    = f.label :tag_list, "e.g. Assent, Sex, Freedom)"
    = f.text_area :tag_list, class: "form-control", rows: "4"
  .actions
    = f.submit 'Save'
